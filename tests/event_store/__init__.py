"""Event store test package (Commit 34 Part 1.1).

The package marker keeps modules such as ``test_repository`` distinct from
other test directories that share the same basename under pytest's non-package
(rootdir) collection mode.
"""
